@model Instructor
@{
    ViewData["Title"] = "Edit";
}



<form method="Post" action="/Instructor/SaveEdit/@Model.Id">
    <div class="form-row">
        <div class="form-group col-md-6">
            <label for="Name">Name</label>
            <input type="text" class="form-control" name="Name" id="Name" value="@Model.Name">
        </div>
        <div class="form-group col-md-6">
            <label for="Salary">Salary</label>
            <input type="text" class="form-control" name="Salary" id="Salary" value="@Model.Salary">
        </div>
    </div>
    <div class="form-group col-md-6">
        <label for="Address">Address</label>
        <input type="text" class="form-control" name="Address" id="Address" value="@Model.Address">
    </div>
    <div class="form-group col-md-6">
        <label for="Imag">Imag</label>
        <input type="text" class="form-control" name="Imag" id="Imag" value="@Model.Imag">
    </div>
    <div class="form-group col-md-4">
        <label for="inputState"> Department Name </label>
      @*   <select name="Dept_Id" id="Dept_Id" class="form-control">
            @foreach (Department DeptID in ViewBag.DeptId)
            {
                if (Model.Dept_Id == DeptID.Id)
                {
                    <option value="@DeptID.Id" selected> @DeptID.Name</option>
                }
                else
                {
                    <option value="@DeptID.Id"> @DeptID.Name</option>
                }
            }
        </select> *@
    </div>

    <div class="form-group col-md-4">
        <label for="inputState"> Course Name </label>
       @*  <select name="Crs_Id" id="Crs_Id" class="form-control">
            @foreach ( Course Crsid in ViewBag.CourseId)
            {
                if (Model.Crs_Id == Crsid.Id)
                {
                    <option value="@Crsid.Id" selected> @Crsid.Name</option>
                }
                else
                {
                    <option value="@Crsid.Id"> @Crsid.Name</option>
                }
            }
        </select> *@
    </div>
    <br>
    <button type="submit" class="btn btn-primary"> Save Edit </button>
</form>